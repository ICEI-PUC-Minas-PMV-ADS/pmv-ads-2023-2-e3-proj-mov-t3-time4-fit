import {Pressable, StyleSheet, Image} from "react-native";

function CloseButton({style, onClose}) {
    return (

    )
}

export default CloseButton;

const styles = StyleSheet.create({
    closeButton: {
        justifyContent: 'center',
        alignItems: 'center',
        marginRight: '20%',
    }
})